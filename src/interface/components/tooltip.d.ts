export interface TooltipProps {
  name: string,
  position?: string,
  color?: string,
  backgroundColor?: string,
  textSize?: string,
  textBold?: boolean,
  capitalize?: boolean,
  children: React.ReactNode,
};
